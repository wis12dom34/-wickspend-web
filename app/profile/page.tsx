"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { useRouter } from "next/navigation";
import { PageShell } from "@/components/PageShell";
import { api } from "@/lib/api";
import { clearSessionToken,getSessionToken } from "@/lib/session";
export default function Profile(){const router=useRouter();const[user,setUser]=useState<any>(null);const[message,setMessage]=useState("Checking account…");const[busy,setBusy]=useState(false);useEffect(()=>{const token=getSessionToken();if(!token){setMessage("You are not signed in.");return}api.auth.session(token).then((data:any)=>{setUser(data?.user||data?.session?.user||data?.data?.user||data?.data||data);setMessage("")}).catch(()=>{clearSessionToken();setMessage("Your session has expired. Please sign in again.")})},[]);async function logout(){const token=getSessionToken();setBusy(true);try{if(token)await api.auth.logout(token)}catch{}finally{clearSessionToken();setBusy(false);router.replace("/login")}}const name=user?.first_name||user?.name||user?.username||user?.telegram_username||"WickSpend account";return <PageShell title="Profile" subtitle="Account and preferences"><div className="panel"><h3>{name}</h3>{user?.telegram_user_id&&<p>Telegram ID: {user.telegram_user_id}</p>}{user?.username&&<p>@{String(user.username).replace(/^@/,"")}</p>}{message&&<p className="statusText">{message}</p>}{!getSessionToken()&&<Link className="secondaryButton" href="/login" style={{marginTop:12}}>Sign in</Link>}</div><div className="list"><Link className="listItem" href="/orders"><span>Orders</span><span>›</span></Link><Link className="listItem" href="/notifications"><span>Notifications</span><span>›</span></Link><Link className="listItem" href="/support"><span>AI Support</span><span>›</span></Link></div>{getSessionToken()&&<div className="panel"><button className="secondaryButton" type="button" disabled={busy} onClick={logout}>{busy?"Signing out…":"Sign out"}</button></div>}</PageShell>}
