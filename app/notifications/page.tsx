"use client";
import { useEffect,useState } from "react";import { PageShell } from "@/components/PageShell";import { api } from "@/lib/api";
export default function Notifications(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{const t=localStorage.getItem("wickspend_token");if(t)api.notifications(t).then((d:any)=>setItems(Array.isArray(d)?d:(d?.notifications||d?.data||[]))).catch(()=>{})},[]);return <PageShell title="Notifications" subtitle="Updates from WickSpend">{items.length?<div className="list">{items.map((n:any,i)=><div className="panel" key={n.id||i}><h3>{n.title||"Update"}</h3><p>{n.message||n.body||""}</p></div>)}</div>:<div className="panel"><h3>You’re all caught up</h3><p>New order, wallet and service updates will appear here.</p></div>}</PageShell>}
