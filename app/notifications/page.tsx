"use client";
import {KeyboardEvent,useEffect,useRef,useState} from "react";import {PageShell} from "@/components/PageShell";import {api} from "@/lib/api";import {getSessionToken} from "@/lib/session";
function formatDate(value:any){if(!value)return"";const d=new Date(value);return Number.isNaN(d.getTime())?"":d.toLocaleString()}
function notificationId(n:any){const id=n?.id??n?.notification_id??n?.notificationId;return id===undefined||id===null||String(id).trim()===""?null:id}
function isNotificationRead(n:any){return n?.read===true||n?.is_read===true||Boolean(n?.read_at)}
export default function Notifications(){const[items,setItems]=useState<any[]>([]),[message,setMessage]=useState("Loading notifications…"),[busy,setBusy]=useState<string|number|"all"|null>(null),loadSeq=useRef(0),actionSeq=useRef(0),mounted=useRef(true);useEffect(()=>()=>{mounted.current=false},[]);async function load(){const seq=++loadSeq.current,t=getSessionToken();if(!t){if(mounted.current&&seq===loadSeq.current){setItems([]);setMessage("Please sign in to view notifications.")}return}setMessage("Loading notifications…");try{const d:any=await api.notifications.list(t);if(!mounted.current||seq!==loadSeq.current)return;const list=Array.isArray(d)?d:(d?.notifications||d?.items||d?.data||[]);setItems(Array.isArray(list)?list:[]);setMessage("")}catch(e){if(!mounted.current||seq!==loadSeq.current)return;setItems([]);setMessage(e instanceof Error?e.message:"Unable to load notifications")}}useEffect(()=>{load()},[]);async function read(id:string|number){if(busy!==null)return;const t=getSessionToken();if(!t){setMessage("Please sign in to update notifications.");return}const seq=++actionSeq.current;setBusy(id);try{await api.notifications.markRead(t,id);if(!mounted.current||seq!==actionSeq.current)return;setItems(c=>c.map(n=>String(notificationId(n))===String(id)?{...n,read:true,is_read:true,read_at:n.read_at||new Date().toISOString()}:n));setMessage("")}catch(e){if(mounted.current&&seq===actionSeq.current)setMessage(e instanceof Error?e.message:"Unable to mark notification as read")}finally{if(mounted.current&&seq===actionSeq.current)setBusy(null)}}async function readAll(){if(busy!==null)return;const t=getSessionToken();if(!t){setMessage("Please sign in to update notifications.");return}const seq=++actionSeq.current;setBusy("all");try{await api.notifications.markAllRead(t);if(!mounted.current||seq!==actionSeq.current)return;const now=new Date().toISOString();setItems(c=>c.map(n=>({...n,read:true,is_read:true,read_at:n.read_at||now})));setMessage("")}catch(e){if(mounted.current&&seq===actionSeq.current)setMessage(e instanceof Error?e.message:"Unable to mark notifications as read")}finally{if(mounted.current&&seq===actionSeq.current)setBusy(null)}}function keyRead(e:KeyboardEvent<HTMLElement>,id:string|number|null,isRead:boolean){if(isRead||busy!==null||id===null)return;if(e.key==="Enter"||e.key===" "){e.preventDefault();read(id)}}const unread=items.filter(n=>!isNotificationRead(n)).length;return <PageShell title="Notifications" subtitle="Updates from WickSpend">{items.length>0&&<div className="notificationTop"><div><strong>{unread}</strong><small>Unread</small></div>{unread>0&&<button type="button" disabled={busy!==null} onClick={readAll}>{busy==="all"?"Updating…":"Mark all read"}</button>}</div>}{items.length?<div className="notificationList" aria-busy={busy!==null}>{items.map((n:any,i)=>{const id=notificationId(n),isRead=isNotificationRead(n),canRead=!isRead&&id!==null,created=formatDate(n.created_at||n.createdAt||n.timestamp);return <article className={isRead?"notificationItem read":"notificationItem"} key={id!==null?String(id):`notification-${i}`} onClick={()=>canRead&&busy===null&&read(id)} onKeyDown={e=>keyRead(e,id,isRead)} role={canRead?"button":undefined} tabIndex={canRead?0:undefined} aria-disabled={canRead&&busy!==null?true:undefined} aria-label={canRead?`Mark ${n.title||"notification"} as read`:undefined}><span className="notificationDot" aria-hidden="true"/><div><div className="notificationTitleRow"><h3>{n.title||"Update"}</h3>{!isRead&&<b>New</b>}</div><p>{n.message||n.body||""}</p>{created&&<small>{created}</small>}</div><span className="notificationChevron" aria-hidden="true">›</span></article>})}</div>:!message?<div className="notificationEmpty"><span aria-hidden="true">🔔</span><h3>You’re all caught up</h3><p>New order, wallet and service updates will appear here.</p></div>:null}{message&&<p className="screenMessage" role="status">{message}</p>}</PageShell>}
