"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";import {getSessionToken,saveSessionToken} from "@/lib/session";
export default function Login(){const router=useRouter(),[telegramId,setTelegramId]=useState(""),[code,setCode]=useState(""),[step,setStep]=useState<"request"|"verify">("request"),[busy,setBusy]=useState(false),[message,setMessage]=useState("");useEffect(()=>{const t=getSessionToken();if(t)api.auth.session(t).then(()=>router.replace("/")).catch(()=>{})},[router]);async function requestCode(e:FormEvent){e.preventDefault();const id=telegramId.trim();if(!id)return setMessage("Enter your Telegram user ID.");setBusy(true);setMessage("Sending verification code…");try{await api.auth.telegramStart({telegram_user_id:id});setStep("verify");setMessage("Verification code sent. Enter the code to continue.")}catch(e){setMessage(e instanceof Error?e.message:"Unable to send verification code")}finally{setBusy(false)}}async function verify(e:FormEvent){e.preventDefault();const id=telegramId.trim(),otp=code.trim();if(!otp)return setMessage("Enter the verification code.");setBusy(true);setMessage("Verifying…");try{const r:any=await api.auth.telegramVerify({telegram_user_id:id,code:otp}),t=r?.session_token||r?.token||r?.data?.session_token||r?.data?.token;if(!t)throw new Error("Session token was not returned after verification.");saveSessionToken(String(t));await api.auth.session(String(t));router.replace("/")}catch(e){setMessage(e instanceof Error?e.message:"Verification failed")}finally{setBusy(false)}}return <main className="authShell"><section className="authCard"><div className="authLogo">W</div><p className="authBrand">WickSpend</p><h1>{step==="request"?"Welcome back":"Verify your account"}</h1><p className="authLead">{step==="request"?"Sign in securely with your Telegram-linked WickSpend account.":`Enter the verification code sent for Telegram ID ${telegramId}.`}</p>{step==="request"?<form className="authForm" onSubmit={requestCode}><label><small>Telegram user ID</small><input inputMode="numeric" autoComplete="off" value={telegramId} onChange={e=>setTelegramId(e.target.value)} placeholder="Enter your Telegram ID"/></label><button disabled={busy}>{busy?"Sending…":"Continue with Telegram"}</button></form>:<form className="authForm" onSubmit={verify}><label><small>Verification code</small><input className="otpInput" inputMode="numeric" autoComplete="one-time-code" value={code} onChange={e=>setCode(e.target.value)} placeholder="000000"/></label><button disabled={busy}>{busy?"Verifying…":"Verify & Sign In"}</button><button className="authTextButton" type="button" onClick={()=>{setStep("request");setCode("");setMessage("")}}>Change Telegram ID</button></form>}{message&&<p className="authMessage">{message}</p>}<p className="authFoot">Secure access to your wallet, purchases and WickSpend services.</p></section></main>}
