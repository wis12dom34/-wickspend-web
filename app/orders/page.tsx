"use client";
import { useEffect,useState } from "react";import { PageShell } from "@/components/PageShell";import { api } from "@/lib/api";
export default function Orders(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{const t=localStorage.getItem("wickspend_token");if(t)api.orders(t).then((d:any)=>setItems(Array.isArray(d)?d:(d?.orders||d?.data||[]))).catch(()=>{})},[]);return <PageShell title="Orders" subtitle="Your purchases and activity">{items.length?<div className="list">{items.map((o:any,i)=><div className="listItem" key={o.id||i}><div><b>{o.service||o.product||"Order"}</b><br/><small>{o.status||"Processing"}</small></div><span>{o.amount_ngn?`₦${Number(o.amount_ngn).toLocaleString()}`:""}</span></div>)}</div>:<div className="panel"><h3>No orders yet</h3><p>Your orders will appear here once you make a purchase.</p></div>}</PageShell>}
